# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Node (not used at runtime — no build step — but keep it clean)
# No trailing slash: the test harness may symlink this in, and `node_modules/`
# does not match a symlink.
node_modules
package-lock.json

# Python bytecode, from importing tools/bake-art.py in a scratch script
__pycache__/
*.pyc

# Local scratch
*.log
tmp/

# Capture output
.shots/
